# LGSale 三機部署與更新

LGDevA、LGDevB、LGSalesOut 使用同一份 Git 程式碼；網址、Port、資料庫帳密、Session secret 與 Cloudflare token 均為各機器本機設定，不進 Git。

## 給其他電腦 Codex 的接手指令

在 LGDevA 或 LGSalesOut Pull 完本次版本後，可直接告訴該電腦的 Codex：

> 請先完整閱讀根目錄的 `三機部署與更新說明.md`，再閱讀 `Codex筆記/整合式人員經銷商庫存巡店資料庫設計規格.md` 的「12.1 三台實體電腦的程式部署與 Git 同步」及「2026-09-03 修改紀錄」。請檢查這台電腦現有環境，依 `.env.example` 建立或補齊本機 `.env.local`，不得複製其他機器的帳密、網址、Session secret 或 Cloudflare token。先備份需要保護的資料庫，依本文件「資料庫 Migration」清單逐項確認並只執行尚未套用的腳本，安裝 requirements，改用 `啟動-LGSale.cmd` 啟動，最後驗證資料庫、Passkey、桌機端、手機端、經銷商端及配對與異動工作台。不要把 `.env.local`、密鑰、token、Log、uploads、runtime 或 `.venv` 加入 Git。

閱讀順序固定為：

1. `三機部署與更新說明.md`：本機部署與操作步驟。
2. `Codex筆記/整合式人員經銷商庫存巡店資料庫設計規格.md`：完整資料語意、UI 與歷史保留規則。
3. `TSQL資料/Migrations/`：依本文件清單核對各台資料庫尚未套用的更新。
4. `.env.example`：本機設定欄位範本，不含可直接使用的帳密。

## 第一次設定

1. `git pull`。
2. 將 `.env.example` 複製為 `.env.local`。
3. 按該機器填入 `LGSALEOUT_ENV`、Port、RP ID、Origin 與資料庫連線資料。
4. 建立 `.venv` 並執行 `.venv\Scripts\python.exe -m pip install -r requirements.txt`。
5. 使用系統管理員身分執行對應的 Cloudflare Tunnel 安裝命令；token 只能放在本機暫存檔，不得加入 Git。
6. 執行 `啟動-LGSale.cmd`。

## 啟動與重新啟動

- 前景啟動／開發除錯：執行 `啟動-LGSale.cmd`；此模式需保留命令視窗。
- 一鍵背景重啟：雙擊 `重新啟動-LGSale.cmd`。它會要求系統管理員權限，依本機 `.env.local` 檢查 Port；若已有程序監聽則先停止，再於背景啟動 LGSale。完成後命令視窗可以關閉。
- 一鍵停止且不重啟：雙擊 `停止-LGSale.cmd`。它會要求系統管理員權限，依本機 `.env.local` 停止監聽程序並確認 Port 已釋放，不會啟動替代程序。資料庫維護或重新匯入測試資料前使用此模式。
- 三台電腦共用上述檔案；不得在檔案內寫死機器名稱、Port、網址或帳密。

## 每次 Pull 後

1. 確認 `.env.local` 仍是本機設定；Git 不會覆蓋此檔。
2. 若 `requirements.txt` 有變更，重新執行 pip install。
3. Git 只會同步 SQL 腳本，不會更新該機器的 SQL Server。先核對 `.env.local` 指向的本機資料庫，依下節在該資料庫執行尚未套用的 Migration；確認成功後才啟動新版程式。
4. 執行 `重新啟動-LGSale.cmd` 自動停止舊程式並於背景重啟；需要查看前景輸出時可執行 `啟動-LGSale.cmd`，開發自動重載可執行 `啟動-LGSale測試模式.cmd`。
5. 開啟 `{LGSALEOUT_ORIGIN}/login/employee`，確認登入頁、桌機版、手機版及經銷商版。

## 資料庫 Migration：三機分別執行

- LGDevA、LGDevB、LGSalesOut 各有自己的資料庫。`git pull` 只帶入 `.sql` 檔，不會把 DevB 已執行的資料庫變更帶到 DevA。
- 目前尚**沒有**「自動辨識並套用待執行 Migration」的功能，也沒有可靠的已執行版本紀錄。不要以為重啟程式會自動建表；同一份 `CREATE TABLE` 腳本也不要直接重複執行。
- 一次性更新腳本集中在 `TSQL資料/Migrations/`，依日期及下表順序檢查。保留已執行的腳本作為歷史紀錄，勿刪除或因搬移檔案而重跑。`_rollback.sql` 是回復用途，不屬於正常更新順序。`TSQL資料/CreateDB.sql` 僅供建立全新資料庫，不可在現有資料庫重跑。
- 在每台尚未套用的機器，先備份需要保護的資料；於 SSMS 確認連到**該機器**的 `LGSaleOut`，並確認查詢視窗選取的資料庫也是 `LGSaleOut`。逐項核對後只執行缺少的更新，成功後才啟動新版程式。尤其 `20260916_權限與Menu.sql` 直接建表，不可重複執行。

| 順序 | 更新腳本（位於 `TSQL資料/Migrations/`） | 套用後核對 |
| --- | --- | --- |
| 1 | `20260903_三機同步與異動工作台.sql` | `DealerTransferReview` 表及經銷商異動相關欄位已存在 |
| 2 | `20260908_期初庫存允許負數.sql` | `MonthlyOpeningInventoryDetail` 的 `CK_MonthlyOpeningInventoryDetail_Quantity` 約束已移除 |
| 3 | `20260908_期初庫存更正歷程.sql` | `OpeningInventoryCorrection` 表已存在 |
| 4 | `20260915_月度陳列照片.sql` | `DealerProductDisplayPhoto` 表已存在；PSI 讀取會查詢此表 |
| 5 | `20260916_權限與Menu.sql` | `PermissionRoleOverride`、`PermissionAccountOverride`、`PermissionDesigner`、`PermissionAudit` 四張表已存在 |
| 6 | `20260917_陳列拍照開關.sql` | `FeatureSetting` 表有 `FeatureKey = 'display_photo'`；`IsEnabled` 初始為 `0`（關閉），之後可由 Designer 切換。關閉時停止新照片上傳與待補提醒，既有照片仍可在 PSI 查閱 |

- 表格是現有資料庫逐台核對的清單，不代表每台都缺全部更新。若已套用或狀態不明，先查資料庫物件與既有紀錄，不要直接重跑。完成後重新整理 PSI 並驗證登入與權限功能。
- `PermissionDesigner` 新表一開始為空。若要王正文擔任該機 Designer，須讓他成為該資料庫建立此表後第一位**成功以員工 Passkey 登入**的人；不同機器各自判定。
- Designer 會自動包含「管理」目前生效的功能與全體經銷商資料範圍，另可使用「權限與 Menu」設定；其原職級的額外權限仍保留。一般「管理」不能設定權限。
- 日後若要更換 Designer，在目標機器先備份資料庫，再於 SSMS 開啟 `TSQL資料/轉移Designer.sql`，填入**新對象的員工編號**並執行。腳本要求新對象是啟用中且已有可用 Passkey 的員工帳號；成功後舊 Designer 立即失效。每台資料庫須各自轉移，不會經 Git 同步這項指定。
- 後續可新增開發機專用的一鍵更新工具：用資料庫版本表記錄每份腳本的名稱與校驗值，只依序執行尚未套用的腳本，成功後同一筆交易記錄版本；正式機維持備份後人工審核、執行。這是規劃，**目前尚未實作**。

## Cloudflare Tunnel

安裝工具：`tools\Install-Cloudflare-Tunnel.ps1`

```powershell
.\tools\Install-Cloudflare-Tunnel.ps1 `
  -ServiceName Cloudflared-LGDevA `
  -SourceToken C:\Temp\tunnel-token.tmp
```

每台機器使用自己的 ServiceName 與 token。安裝完成後刪除暫存 token；程式碼資料夾內不得保存 token。

## 正式機規則

- LGSalesOut 只 Pull 已確認的 `main` 或正式版本標籤。
- 正式資料庫 Migration 必須先備份並人工執行，不在程式啟動時自動修改。
- 不得將 `.env.local`、`.lgsale-session-secret`、`.venv`、`runtime`、Log、上傳檔或 Tunnel token Push 到 Git。
