# LGSale 三機部署與更新

LGDevA、LGDevB、LGSalesOut 使用同一份 Git 程式碼；網址、Port、資料庫帳密、Session secret 與 Cloudflare token 均為各機器本機設定，不進 Git。

## 給其他電腦 Codex 的接手指令

在 LGDevA 或 LGSalesOut Pull 完本次版本後，可直接告訴該電腦的 Codex：

> 請先完整閱讀根目錄的 `三機部署與更新說明.md`，再閱讀 `Codex筆記/整合式人員經銷商庫存巡店資料庫設計規格.md` 的「12.1 三台實體電腦的程式部署與 Git 同步」及「2026-09-03 修改紀錄」。請檢查這台電腦現有環境，依 `.env.example` 建立或補齊本機 `.env.local`，不得複製其他機器的帳密、網址、Session secret 或 Cloudflare token。先備份需要保護的資料庫，再執行尚未套用的 `TSQL資料/Migrations/20260903_三機同步與異動工作台.sql`，安裝 requirements，改用 `啟動-LGSale.cmd` 啟動，最後驗證資料庫、Passkey、桌機端、手機端、經銷商端及配對與異動工作台。不要把 `.env.local`、密鑰、token、Log、uploads、runtime 或 `.venv` 加入 Git。

閱讀順序固定為：

1. `三機部署與更新說明.md`：本機部署與操作步驟。
2. `Codex筆記/整合式人員經銷商庫存巡店資料庫設計規格.md`：完整資料語意、UI 與歷史保留規則。
3. `TSQL資料/Migrations/20260903_三機同步與異動工作台.sql`：本次既有資料庫更新內容。
4. `.env.example`：本機設定欄位範本，不含可直接使用的帳密。

## 第一次設定

1. `git pull`。
2. 將 `.env.example` 複製為 `.env.local`。
3. 按該機器填入 `LGSALEOUT_ENV`、Port、RP ID、Origin 與資料庫連線資料。
4. 建立 `.venv` 並執行 `.venv\Scripts\python.exe -m pip install -r requirements.txt`。
5. 使用系統管理員身分執行對應的 Cloudflare Tunnel 安裝命令；token 只能放在本機暫存檔，不得加入 Git。
6. 執行 `啟動-LGSale.cmd`。

## 每次 Pull 後

1. 確認 `.env.local` 仍是本機設定；Git 不會覆蓋此檔。
2. 若 `requirements.txt` 有變更，重新執行 pip install。
3. 依設計規格的 Migration 清單，在該機器對應的資料庫執行尚未套用的 SQL。
4. 關閉舊程式並執行 `啟動-LGSale.cmd`；開發模式可執行 `啟動-LGSale測試模式.cmd`。
5. 開啟 `{LGSALEOUT_ORIGIN}/login/employee`，確認登入頁、桌機版、手機版及經銷商版。

## Cloudflare Tunnel

安裝工具：`tools\Install-Cloudflare-Tunnel.ps1`

```powershell
.\tools\Install-Cloudflare-Tunnel.ps1 `
  -ServiceName Cloudflared-LGDevA `
  -SourceToken C:\Temp\tunnel-token.tmp
```

每台機器使用自己的 ServiceName 與 token。安裝完成後刪除暫存 token；程式碼資料夾內不得保存 token。

## 正式機規則

- LGSalesOut 只 Pull 已確認的 `main` 或正式版本標籤。
- 正式資料庫 Migration 必須先備份並人工執行，不在程式啟動時自動修改。
- 不得將 `.env.local`、`.lgsale-session-secret`、`.venv`、`runtime`、Log、上傳檔或 Tunnel token Push 到 Git。
