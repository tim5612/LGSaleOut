# Application

正式版應用程式程式碼目錄。

- `routes/`：HTTP API 與頁面路由。
- `services/`：業務規則與交易流程。
- `repositories/`：SQL Server 資料存取。
- `templates/`：伺服器端頁面模板。
- `static/`：前端 JavaScript、CSS 與靜態資源。

目前只有目錄骨架，尚未開始正式版程式實作。
